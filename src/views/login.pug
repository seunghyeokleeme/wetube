extends base

block content
    form(action="/login", method="POST")
        input(type="text", placeholder="아이디", name="username", required)
        input(type="password", placeholder="비밀번호 입력하세요",name="password", required)
        input(type="submit", value="로그인")
        br
        a(href="/users/github/start") Continue with Github
        br
        a(href="/users/kakao/start") Continue with Kakao
    hr
    div 
        span Don't have an account?
        a(href="/join") Create one now &rarr;